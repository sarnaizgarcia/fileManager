export enum Sizes {
    BIG = 'big', 
    SMALL = 'small'
}

export enum Types {
    BUTTON = 'button',
    SUBMIT = 'submit'
}

export enum Color {
    PRIMARY = 'primary',
    SECONDARY = 'secondary'
}